#include <stdio.h>

int main(){
    int cases;

    while (cases--) {
        unsigned num_a, num_b;
        char nome_a, nome_b, paridade_a, paridade_b;

        scanf("%s %s %s %s", &nome_a, &paridade_a, &nome_b, &paridade_b);
        scanf("%u %u", &num_a, &num_b);

        if ((num_a+num_b) % 2 == 0) {
        
        }
        
    }





    return 0;
}